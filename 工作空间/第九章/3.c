#include <stdio.h>
void  swap ( int  *p,  int  *q )
{
    int  t;
    t = *p; 
	;
    *q = t;
}
int main()
{
    int  a, b, c;
    scanf ( "%d%d%d", &a, &b, &c );

    if ( a < b )
        if ( a < c )
        { printf ( "%d\n", ); }

    return 0;
}

