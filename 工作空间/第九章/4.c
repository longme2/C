#include <stdio.h>
int main()
{
    int  a[5] = {1, 2, 3, 4, 5,};
    int   s, *p ;
    s = 1;

    for ( p = a; ________; p++ )
    { _____________; }

    printf ( "s=%d\n", s );
    return 0;
}
